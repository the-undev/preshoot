# preshoot

Storyboard and prompt builder for local and Runpod video and image generation.

## Setup

```bash
pnpm install
pnpm dev
```

`pnpm check` runs lint, format check, typecheck and tests. `pnpm build:linux` produces an AppImage.
